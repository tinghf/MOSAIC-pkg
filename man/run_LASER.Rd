% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_LASER.R
\name{run_LASER}
\alias{run_LASER}
\alias{run_laser}
\title{Run LASER Model Simulation}
\usage{
run_LASER(
  config,
  seed = NULL,
  quiet = FALSE,
  visualize = FALSE,
  pdf = FALSE,
  outdir = tempdir(),
  py_module = NULL
)

run_laser(
  config,
  seed = NULL,
  quiet = FALSE,
  visualize = FALSE,
  pdf = FALSE,
  outdir = tempdir(),
  py_module = NULL
)
}
\arguments{
\item{config}{Character or list. Either a path to a LASER configuration file (YAML/JSON) or a configuration list object.}

\item{seed}{Integer or NULL. Random seed for reproducibility. If NULL (default), uses \code{config$seed} when present, otherwise defaults to 123L.}

\item{quiet}{Logical. If TRUE, suppress the progress bar during model execution. Defaults to FALSE.}

\item{visualize}{Logical. If TRUE, generate and display visualizations during the run. Defaults to FALSE.}

\item{pdf}{Logical. If TRUE, save visualizations as PDF files. Defaults to FALSE.}

\item{outdir}{Character. Directory where LASER outputs (e.g., logs, results) will be written. Defaults to a temporary directory.}

\item{py_module}{Python module. An optional pre-loaded reticulate LASER module. If NULL, the module is imported via reticulate::import("laser_cholera.metapop.model").}
}
\value{
A Python object (reticulate) representing the LASER model simulation results.
}
\description{
A wrapper function to execute a LASER model simulation via the Python interface (reticulate).
Automatically suppresses NumPy divide-by-zero warnings that can occur during vaccination
calculations when susceptible and exposed compartments are both zero.
}
\examples{
\dontrun{
# Run with config file path:
result <- run_LASER(
  config    = "path/to/laser_params.yml",
  seed      = 20250418L,
  quiet     = FALSE,
  visualize = FALSE,
  pdf       = FALSE,
  outdir    = "./laser_output"
)

# Run with config object (uses config$seed if present, else 123L):
result <- run_LASER(
  config = config_default,
  quiet  = TRUE
)

# Inspect results:
print(result)
}

}
